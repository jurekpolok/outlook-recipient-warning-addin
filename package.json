{
  "name": "outlook-recipient-warning-addin",
  "version": "1.0.0",
  "description": "Outlook Add-in that warns users when adding more than 5 recipients and suggests using BCC for privacy",
  "main": "src/taskpane.js",
  "scripts": {
    "start": "npx http-server . -p 3000 --cors -S -C localhost.crt -K localhost.key",
    "start-http": "npx http-server . -p 3000 --cors"
  },
  "keywords": [
    "outlook",
    "add-in",
    "privacy",
    "bcc"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
